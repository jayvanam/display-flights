# Never let the scraper's data or secrets into this PUBLIC repo.
*.db
*.db-wal
*.db-shm
*.db.gz
.env
.env.*

# Cron logs (publish.log, sync.log) — this repo is pulled/rebased every 20 min by
# publish.sh, so stray untracked files here are noise at best.
*.log
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
.DS_Store
